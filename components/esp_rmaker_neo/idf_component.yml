version: "0.8.0"
description: "ESP RainMaker Neo Firmware SDK: node data model, provisioning, services and cloud connectivity."
license: "Apache-2.0"
dependencies:
  espressif/network_provisioning: ^1.0.5
  esp_rmaker_neo_common:
    path: ../esp_rmaker_neo_common
  osal:
    path: ../osal
  socket_mocks:
    path: ./fault_injection/socket_mocks

  # external dependencies
  espressif/esp_schedule: ~1.5.0 # scheduling backend
  espressif/rmaker_console: ^1.0.0 # common serial console + commands
