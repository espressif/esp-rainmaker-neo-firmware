dependencies:
  # RMNG SDK + OTA
  esp_rmaker_neo:
    path: ../../../../components/esp_rmaker_neo
  esp_rmaker_neo_ota:
    path: ../../../../components/esp_rmaker_neo_ota
  # ESP-only network bring-up + RMNG provisioning
  app_network:
    path: ../../../../examples/common/app_network
  # Platform entry-point shim
  app_entry:
    path: ../../../../examples/common/app_entry
